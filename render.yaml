services:
  - type: web
    name: knowledge-base-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GEMINI_API_KEY
        value: your_google_api_key_here
      - key: UPLOAD_FOLDER
        value: /opt/render/knowledge/